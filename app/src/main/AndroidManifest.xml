<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.garotadpi.fitoform">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" />
        <activity android:name=".CadastroActivity" />
        <activity android:name=".CadeiraAActivity" />
        <activity android:name=".CadeiraBActivity" />
        <activity android:name=".CadeiraCActivity" />
        <activity android:name=".CadeiraDActivity" />
        <activity android:name=".CadeiraEActivity" />
        <activity android:name=".CadeiraFActivity" />
        <activity android:name=".CadeiraGActivity" />
        <activity android:name=".FinalActivity"></activity>
    </application>

</manifest>
